# Install — OpenAI Codex CLI

Append the content of `cognitive-protocol.md` to `AGENTS.md` in your project root:

```bash
cat /path/to/auto-learning/cognitive-base/cognitive-protocol.md >> AGENTS.md
```

Or add it to your global Codex instructions file.
