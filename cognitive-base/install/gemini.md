# Install — Gemini CLI

Add the content of `cognitive-protocol.md` to your `GEMINI.md` file in the project root or home directory:

```bash
cat /path/to/auto-learning/cognitive-base/cognitive-protocol.md >> GEMINI.md
```

Or set it in the `system_instruction` field of your Gemini configuration.
